import TranslationEngine from "@/components/TranslationEngine";

export const metadata = { title: "Translation Engine — Leer Nederlands" };

export default function TranslationPage() {
  return <TranslationEngine />;
}
