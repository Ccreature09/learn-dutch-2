import FlashcardSystem from "@/components/FlashcardSystem";

export const metadata = { title: "Flashcards — Leer Nederlands" };

export default function FlashcardsPage() {
  return <FlashcardSystem />;
}
